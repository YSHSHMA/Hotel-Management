services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hotel-frontend
    ports:
      - "3001:5174"
    volumes:
      - .:/app
      - /app/node_modules
    stdin_open: true
    tty: true